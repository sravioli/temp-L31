MODULO PER LA GESTIONE DEI SALVATAGGI
=====================================

manca menù

FUNZIONE save_game()
====================

Questa funzione scrive su file binario una partita in corso.

INPUT:
- players, struttura con i giocatori del gioco, Players;
- board, tabellone di gioco, Board;

OUTPUT:
- nullo, non ha output.

LAVORO:
- game, struttura di salvataggio del gioco, Game;


ALGORITMO:
SE (get_num_games(games) < MAX_GAMES) // 5
  ALLORA chedere all'utente che partita eliminare // choose_saved_game()
  ALTRIMENTI
    scrivere normalmente nella struct (num_games + 1)
    game := set_game_players(game, players)
    game := set_game_board(game, board)

    game_name := LeggereDaTastiera()
    game := set_game_name(game, game_name)
    str_concat(game_name, ".bin")

    ScrivereSuFile(game, game_name)
FINE


FUNZIONE delete_game()
======================

// chiedere all'utente che partita eliminare (indice utente) // funzione a parte
indice utente := choose_saved_game()
copia:
itera su Games
 se indice == indice utente
  indice := indice + 1 // skip
 fine
settare numero Games

scrivere alla posizione fornita dall'utente la partita.


FUNZIONE choose_saved_game()
============================

chiede all'utente che partita salvata vuole, ritorna l'indice.


FUNZIONE load_game()
====================

ALGORITMO:
indice := choose_saved_game()
load_game(games, indice)


FUNZIONE load_game()
====================

ALGORITMO:
copia Players in struct a parte
copia Board in struct a parte
launch_new_game(players, board)
